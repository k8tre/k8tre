apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - certificate.yaml
  - netpol_singleuser.yaml
  - network_policy.yaml

patches:
  - path: patch-rolebinding.yaml
    target:
      kind: RoleBinding
      name: hub
