# Dev-specific values
singleuser:
  image:
    name: jupyter/minimal-notebook
    tag: latest
  
  resources:
    requests:
      cpu: 0.2
      memory: 512Mi
    limits:
      cpu: 1
      memory: 1Gi

proxy:
  service:
    type: ClusterIP
