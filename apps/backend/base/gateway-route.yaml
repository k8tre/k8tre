apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: backend-route
  namespace: backend
spec:
  parentRefs:
  - name: internal-gateway
    namespace: gateway
  hostnames:
    - "backend.${ENVIRONMENT}.${DOMAIN}"
  rules:
    - backendRefs:
        - name: backend
          namespace: backend
          port: 80
